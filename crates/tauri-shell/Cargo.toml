[package]
name = "tauri-shell"
version = "0.1.0"
edition = "2021"

[dependencies]
scada-core = { path = "../scada-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
